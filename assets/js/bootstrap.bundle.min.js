/*!
 * Bootstrap v5.2.3 (https://getbootstrap.com/)
 * Este archivo contendría todo el JavaScript minificado de Bootstrap 5.2.3
 * Para un laboratorio de auditoría, puedes descargar el archivo completo desde:
 * https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js
 */

!(function (e, t) {
  "object" == typeof exports && "undefined" != typeof module
    ? (module.exports = t())
    : "function" == typeof define && define.amd
    ? define(t)
    : ((e =
        "undefined" != typeof globalThis ? globalThis : e || self).bootstrap =
        t());
})(this, function () {
  "use strict";
  const e = "transitionend",
    t = (e) => {
      let t = e.getAttribute("data-bs-target");
      if (!t || "#" === t) {
        let i = e.getAttribute("href");
        if (!i || (!i.includes("#") && !i.startsWith("."))) return null;
        i.includes("#") && !i.startsWith("#") && (i = `#${i.split("#")[1]}`),
          (t = i && "#" !== i ? i.trim() : null);
      }
      return t;
    },
    i = (e) => {
      const i = t(e);
      return i && document.querySelector(i) ? i : null;
    },
    n = (e) => {
      const i = t(e);
      return i ? document.querySelector(i) : null;
    };
  return bootstrap;
});

// Nota: Este es solo un extracto muy pequeño de Bootstrap para demostración
// En un entorno real, usarías el archivo completo
